apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: rentdrive-hpa
  namespace: rentdrive
  labels:
    app: rentdrive
    tier: frontend
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rentdrive-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 75
